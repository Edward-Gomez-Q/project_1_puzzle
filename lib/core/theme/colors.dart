import 'package:flutter/material.dart';

class AppColors {
  // Light Theme
  static const Color lightPrimary = Color(
    0xFF3B82F6,
  ); // Azul vibrante (indigo/azul moderno)
  static const Color lightSecondary = Color(0xFF6366F1); // Indigo suave
  static const Color lightAccent = Color(
    0xFFFF6B6B,
  ); // Coral/rojo pastel (acentos, botones)
  static const Color lightBackground = Color(
    0xFFF9FAFB,
  ); // Casi blanco (neutro moderno)
  static const Color lightSurface = Color(
    0xFFE5E7EB,
  ); // Gris claro (tarjetas, inputs)
  static const Color lightTextPrimary = Color(
    0xFF111827,
  ); // Gris muy oscuro (texto principal)
  static const Color lightTextSecondary = Color(
    0xFF6B7280,
  ); // Gris medio (texto secundario)
  static const Color lightCardBackground = Color(0xFFFFFFFF);

  // Dark Theme
  static const Color darkPrimary = Color(0xFF2563EB); // Azul fuerte
  static const Color darkSecondary = Color(0xFF4F46E5); // Indigo fuerte
  static const Color darkAccent = Color(
    0xFFFF5C5C,
  ); // Coral claro (acentos vivos)
  static const Color darkBackground = Color(
    0xFF0F172A,
  ); // Azul muy oscuro, casi negro
  static const Color darkSurface = Color(0xFF1E293B); // Gris azulado oscuro
  static const Color darkTextPrimary = Color(0xFFF1F5F9); // Blanco azulado
  static const Color darkTextSecondary = Color(
    0xFF94A3B8,
  ); // Gris claro azulado
  static const Color darkCardBackground = Color(0xFF1C1C1E);
}
